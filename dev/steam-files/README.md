# steam-files

Fast TUI picker for Steam game directories (compatdata/proton prefixes).

## Install

```sh
cargo install --path .
```

## Usage

Run `steam-files` to open the picker, select a game, and cd into its compatdata directory.

- Type to filter
- `j`/`k` or arrows to navigate
- `Enter` to select
- `Esc` to cancel
